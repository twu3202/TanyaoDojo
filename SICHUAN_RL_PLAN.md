# 川麻(血战到底)纯 RL 方案(2026-07-24 立项设计)

**方向更新**:中国地方麻将从"归档不做"转为**立项**(用户 2026-07-24)。选型 = 四川麻将血战到底,
理由(前期调研 CHINA_MJ_*.md + 本次论证):**一局最多 3 家胡 + 杠即时结算(刮风下雨)+ 查叫/退税
→ 奖励天然稠密,纯 RL 不需要 GRP 类奖励模型**;规则比立直简单(108 张无字牌、无吃、无役种体系)、
动作空间小 → tabula rasa 可行性高;且无人类数据集 → 纯 RL 是必然路径。

## 核心架构:GPU 原生向量化环境(fork Mahjax)

- 基底:**Mahjax**(github.com/nissymori/mahjax,JAX+vmap/jit,riichi 全规则,8×A100 实测
  1-2M 步/秒,自带 BC+PPO 示例,Apache 2.0)。
- **川麻相对立直是"删规则"为主**:删吃、删字牌(136→108)、删立直/役/宝牌(换番型倍率表:
  平胡/对对/清一色/七对/杠上花/抢杠/海底...×根)。
- 需新增:定缺(换三张 + 缺一门约束进合法动作掩码)、**多胡续行**(胡家离场余家继续,状态机
  最大改动)、杠即时分转移、终局查大叫/退税、(可选)呼叫转移。
- 环境/策略推理/训练同进程同 GPU,零序列化零 TCP——对比现值线管线(CPU 引擎 + actor-learner
  分布式)是 100× 级架构换代。

## 算法

- **PPO 自对弈**(4 座参数共享)+ 熵正则;on-policy + 裁剪自带信赖域 → 值线撞过的
  churn/外推退化在此范式结构性不存在(教训见 TRACK_C_SELFPLAY.md 复盘)。
- 奖励 = 每局分数转移(零和,天然稠密,含局中杠结算)。不需要 GRP。
- 网络小起步(obs 简单,64-128ch CNN 或轻 transformer;Mahjax 两种 obs 皆支持)。
- 可选加速:Suphx 式 oracle critic(透视信息训 value,策略头不透视)。
- 对手多样性:先纯自对弈,后快照 league(复用现对手池运维经验)。

## 正确性策略(最高优先级,值线血泪教训)

1. 规则文档冻结(复活 CHINA_MJ_JIAXING/VARIANTS 调研,明确采用的番型表与结算细则)。
2. **朴素 Python 参考实现**作 oracle(不求快,求对)。
3. JAX 版与参考实现**差分测试**(百万随机对局逐决策点比对)+ 不变量断言(分数零和守恒、
   牌数守恒、掩码一致)+ 边角用例集(呼叫转移/查大叫/退税/抢杠/杠上炮...)。
4. 环境 bug 会无声毒化 RL——差分测试通过前不开训练。

## 算力方案(云)

- 开发:AutoDL 4090(账号现成)——env 开发/差分测试/小规模训练。
- 冲量:A100/8×A100 短租;按 Mahjax 吞吐,**1e8 局 = 卡·天量级**,川麻 v1 总云成本估数千元。
- 现有本地两卡(RTX 6000 Ada/5060 Ti)可承担开发与评测,不依赖常驻云。

## 阶段与里程碑

- P0(1-2周):规则冻结 + Python 参考实现 + 用例集。
- P1(2-3周):Mahjax fork 改川麻 + 差分验证通过 + 单 4090 吞吐基准。
- P2(1周):PPO harness(参考 Mahjax examples/purejaxrl)小规模跑通,学习曲线上扬。
- P3:租卡冲量 + league + 评测阶梯:规则 bot → 贪心/搜索 bot → 人类对照(雀魂血战模式)。
- 运维件(哨兵/里程碑体检/落袋快照)自现项目平移。

## 与立直线的关系

- **同栈红利**:P1 的 JAX 向量化能力 = 立直线最后 ~1.9pt 的 PPO 方案(RL_SIM_PLAN 后手牌)
  的前置技能,一次投资两线受益。
- 现值线结论(截至 C2):锚定可保值不可增值,rl1_best = -1.85~-1.96±0.53;若 C2 判平,
  立直线的下一步即 AWR/PPO,与川麻线共享基建。
- **不为值线租卡横扩**(worker 扩展撞 trainer 墙,全系统 ≤2-3×,不值)。
